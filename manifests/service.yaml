apiVersion: v1
kind: Service
metadata:
  name: testworkflow3
  namespace: namespace-workflow-1718913604982
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: testworkflow3
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080